import { ContrasennaPipe } from './contrasenna.pipe';

describe('ContrasennaPipe', () => {
  it('create an instance', () => {
    const pipe = new ContrasennaPipe();
    expect(pipe).toBeTruthy();
  });
});
